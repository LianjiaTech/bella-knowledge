bella:
  file-api:
    file-entry:
      read-mode: closure
      write-mode: dual
      cross-space-move-enabled: false
      cross-space-move-warn-threshold: 5000
    file:
      tmp-file-dir: .

apollo:
  bootstrap:
    enabled: true
